% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match.R
\name{pgscatalog_match}
\alias{pgscatalog_match}
\title{Fuzzy-match ancestry labels to PGS Catalog acronyms}
\usage{
pgscatalog_match(labels, max_dist = 3, quiet = FALSE)
}
\arguments{
\item{labels}{Character vector of ancestry labels to match. Values may be
acronyms (e.g., \code{"eur"}, \code{"Eur"}), full names (e.g.,
\code{"european"}, \code{"East_Asian"}), or anything in between.}

\item{max_dist}{Integer. Maximum edit distance to accept as a fuzzy match.
Lower values are stricter. Default: \code{3}. Set to \code{0} to disable fuzzy
matching and accept only exact matches.}

\item{quiet}{Logical. Suppress warnings about fuzzy matches and failures?
Default: \code{FALSE}.}
}
\value{
A character vector the same length as \code{labels} containing PGS Catalog
acronyms, or \code{NA} where no match was found.
}
\description{
Attempts to map a character vector of ancestry labels to their PGS Catalog
acronyms. Matching proceeds in three steps:
}
\details{
\enumerate{
\item \strong{Exact match} against known acronyms (case-insensitive).
\item \strong{Exact match} against full ancestry names (case-insensitive).
\item \strong{Fuzzy match} using edit distance against both acronyms and full names,
accepting the closest match within \code{max_dist} edits.
}

Unresolved labels (no match within \code{max_dist}) are returned as \code{NA} and
always trigger a warning.
}
\examples{
pgscatalog_match(c("EUR", "eur", "european", "East Asian", "east_asian"))
pgscatalog_match(c("Europeen", "SAS", "unknown_group"), max_dist = 3)

# Typical use: add a matched acronym column then plot
df <- data.frame(
  label = c("European", "african unspecified", "EAST ASIAN", "sas"),
  value = c(1.2, 0.8, 1.5, 0.9)
)
df$acronym <- pgscatalog_match(df$label)
}
\seealso{
\code{\link[=pgscatalog_ancestry_info]{pgscatalog_ancestry_info()}} for the full lookup table.
}
