% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tints.R
\name{scales_pgscatalog_tints}
\alias{scales_pgscatalog_tints}
\title{Paired colour + fill scales for lines with 95\% CI ribbons}
\usage{
scales_pgscatalog_tints(
  acronym,
  n = 5,
  labels = NULL,
  direction = c("light_to_dark", "dark_to_light"),
  range = c(0.35, 0.85),
  ribbon_alpha = 0.35,
  ...
)
}
\arguments{
\item{acronym}{Character scalar. A single PGS Catalog ancestry acronym
(e.g., \code{"EUR"}, \code{"AFR"}). See \code{\link[=pgscatalog_ancestry_info]{pgscatalog_ancestry_info()}} for valid
values.}

\item{n}{Integer. Number of color variants to return. Default: \code{5}.}

\item{labels}{Character vector of length \code{n} to use as names on the
returned vector. Useful for direct use in
\code{\link[ggplot2:scale_color_manual]{ggplot2::scale_color_manual()}}. If \code{NULL} (default), variants are named
\code{1} through \code{n}.}

\item{direction}{One of \code{"light_to_dark"} (default) or \code{"dark_to_light"}.
Controls which end of the ramp maps to the first returned color.}

\item{range}{Numeric vector of length 2, both values between 0 and 1.
Defines how far the ramp extends from pure white (0) to near-black (1).
Default \code{c(0.2, 0.85)} keeps variants readable while preserving the
ancestry hue.}

\item{ribbon_alpha}{Numeric between 0 and 1. Alpha applied to the fill colors
for ribbons. Default: \code{0.35}.}

\item{...}{Additional arguments passed to both
\code{\link[ggplot2:scale_color_manual]{ggplot2::scale_color_manual()}} and \code{\link[ggplot2:scale_fill_manual]{ggplot2::scale_fill_manual()}}.}
}
\value{
A list of two ggplot2 scale objects.
}
\description{
Returns a list of two ggplot2 scales — one for \code{colour} (lines) and one for
\code{fill} (ribbons) — using the same tint ramp. The fill colors have alpha
baked in via 8-digit hex so \code{geom_ribbon} can be drawn with \code{alpha = 1},
avoiding the colour washout that occurs when semi-transparent light tints
are blended against a white background.
}
\details{
Add the result directly to a ggplot with \code{+} (ggplot2 accepts lists of
scales).
}
\examples{
library(ggplot2)
set.seed(1)
df <- data.frame(
  time     = rep(seq(0, 10, length.out = 50), 5),
  risk     = unlist(lapply(1:5, function(q)
               pnorm(seq(-2, 2, length.out = 50), mean = q - 3))),
  lower    = unlist(lapply(1:5, function(q)
               pnorm(seq(-2, 2, length.out = 50), mean = q - 3) - 0.05)),
  upper    = unlist(lapply(1:5, function(q)
               pnorm(seq(-2, 2, length.out = 50), mean = q - 3) + 0.05)),
  quintile = factor(rep(paste0("Q", 1:5), each = 50))
)
ggplot(df, aes(time, risk, colour = quintile, fill = quintile)) +
  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 1, colour = NA) +
  geom_line(linewidth = 1) +
  scales_pgscatalog_tints("EUR", n = 5, labels = paste0("Q", 1:5)) +
  theme_classic()
}
