% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tints.R
\name{pgscatalog_tints}
\alias{pgscatalog_tints}
\title{Generate tints and shades from a single ancestry color}
\usage{
pgscatalog_tints(
  acronym,
  n = 5,
  labels = NULL,
  direction = c("light_to_dark", "dark_to_light"),
  range = c(0.2, 0.85)
)
}
\arguments{
\item{acronym}{Character scalar. A single PGS Catalog ancestry acronym
(e.g., \code{"EUR"}, \code{"AFR"}). See \code{\link[=pgscatalog_ancestry_info]{pgscatalog_ancestry_info()}} for valid
values.}

\item{n}{Integer. Number of color variants to return. Default: \code{5}.}

\item{labels}{Character vector of length \code{n} to use as names on the
returned vector. Useful for direct use in
\code{\link[ggplot2:scale_color_manual]{ggplot2::scale_color_manual()}}. If \code{NULL} (default), variants are named
\code{1} through \code{n}.}

\item{direction}{One of \code{"light_to_dark"} (default) or \code{"dark_to_light"}.
Controls which end of the ramp maps to the first returned color.}

\item{range}{Numeric vector of length 2, both values between 0 and 1.
Defines how far the ramp extends from pure white (0) to near-black (1).
Default \code{c(0.2, 0.85)} keeps variants readable while preserving the
ancestry hue.}
}
\value{
A named character vector of \code{n} hex color codes.
}
\description{
When multiple curves or groups all belong to the same ancestry (e.g., PGS
quintiles, age strata, or sex-stratified cumulative risk curves), this
function returns \code{n} perceptually ordered color variants derived from that
ancestry's base color. Variants range from light (tint) to dark (shade),
with the canonical PGS Catalog color anchored at the midpoint.
}
\details{
Colors are generated by interpolating through HCL space between a lightened
and darkened version of the base color, keeping the hue constant. The
\code{range} argument controls how far the ramp extends away from the base color
at each end (0 = base color only, 1 = pure white / near-black).

\strong{Practical limits on \code{n}:} perceptual distinctiveness (CIEDE2000 delta-E)
between adjacent variants decreases as \code{n} grows. Approximate guidance:
\describe{
\item{n <= 5}{delta-E > 11 — clearly distinct in any context.}
\item{n = 6-8}{delta-E 6–10 — distinguishable with thick lines; consider
direct labels for small plot elements.}
\item{n = 9-10}{delta-E 5–6 — usable when curves are visually separated
(e.g., cumulative risk curves that fan apart); harder in legends.}
\item{n > 10}{delta-E < 5 — adjacent variants may be confused; a warning
is issued.}
}
}
\examples{
# Five EUR variants light-to-dark (for 5 PGS quintiles)
pgscatalog_tints("EUR", n = 5)

# Three AFR variants with group labels
pgscatalog_tints("AFR", n = 3, labels = c("Low", "Mid", "High"))

# Flip direction so darkest comes first
pgscatalog_tints("EAS", n = 4, direction = "dark_to_light")
}
\seealso{
\code{\link[=scale_color_pgscatalog_tints]{scale_color_pgscatalog_tints()}}, \code{\link[=pgscatalog_cols]{pgscatalog_cols()}}
}
