% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tints.R
\name{scale_fill_pgscatalog_tints}
\alias{scale_fill_pgscatalog_tints}
\title{ggplot2 fill scale using ancestry tints}
\usage{
scale_fill_pgscatalog_tints(
  acronym,
  n = 5,
  labels = NULL,
  direction = c("light_to_dark", "dark_to_light"),
  range = c(0.2, 0.85),
  ...
)
}
\arguments{
\item{acronym}{Character scalar. A single PGS Catalog ancestry acronym
(e.g., \code{"EUR"}, \code{"AFR"}). See \code{\link[=pgscatalog_ancestry_info]{pgscatalog_ancestry_info()}} for valid
values.}

\item{n}{Integer. Number of color variants to return. Default: \code{5}.}

\item{labels}{Character vector of length \code{n} to use as names on the
returned vector. Useful for direct use in
\code{\link[ggplot2:scale_color_manual]{ggplot2::scale_color_manual()}}. If \code{NULL} (default), variants are named
\code{1} through \code{n}.}

\item{direction}{One of \code{"light_to_dark"} (default) or \code{"dark_to_light"}.
Controls which end of the ramp maps to the first returned color.}

\item{range}{Numeric vector of length 2, both values between 0 and 1.
Defines how far the ramp extends from pure white (0) to near-black (1).
Default \code{c(0.2, 0.85)} keeps variants readable while preserving the
ancestry hue.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:scale_fill_manual]{ggplot2::scale_fill_manual()}}.}
}
\value{
A ggplot2 scale object.
}
\description{
Convenience wrapper around \code{\link[ggplot2:scale_fill_manual]{ggplot2::scale_fill_manual()}} that applies the
tint ramp for a single ancestry group.
}
\examples{
library(ggplot2)
df <- data.frame(
  group = factor(paste0("Q", 1:5), levels = paste0("Q", 1:5)),
  value = c(0.10, 0.18, 0.28, 0.40, 0.55)
)
ggplot(df, aes(group, value, fill = group)) +
  geom_col() +
  scale_fill_pgscatalog_tints("SAS", n = 5, labels = paste0("Q", 1:5)) +
  theme_classic()
}
