# pgscatalogcolors 0.1.0.9000 (alpha)

# pgscatalogcolors 0.1.0

* Initial release.
* Provides 19 PGS Catalog ancestry acronym-to-color mappings.
* Includes `scale_color_pgscatalog()` and `scale_fill_pgscatalog()` for ggplot2.
* `pgscatalog_ancestry_info()` returns the full acronym/name/color lookup table.
